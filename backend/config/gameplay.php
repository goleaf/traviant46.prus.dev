<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Evasion Return Delay
    |--------------------------------------------------------------------------
    |
    | When troops evade an incoming attack they return home after a fixed delay.
    | The legacy engine expresses this in seconds, so we keep the same unit here
    | to make migrations straightforward.
    |
    */
    'evasion_return_seconds' => 900,
];

